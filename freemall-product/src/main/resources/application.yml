spring:
  application:
    name: freemall-product
  config:
    import:
      - optional:nacos:${spring.application.name}.yaml
  cloud:
    nacos:
      discovery:
        namespace: dev
        group: freemall
      config:
        file-extension: yaml
      server-addr: 127.0.0.1:8848